# CrypToCare — Frontend environment variables
# Copy this file to .env.development (or .env) and adjust as needed.

# Backend API URL — must include the trailing /api/ path
VITE_API_URL=http://localhost:8001/api/
